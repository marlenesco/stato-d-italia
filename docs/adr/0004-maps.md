# ADR 0004: static PMTiles maps

Status: accepted.

ISTAT generalized geometry is transformed to static PMTiles. PMTiles is geometry
delivery only; thematic values remain independent release assets keyed by territory
ID/year. MapLibre joins values client-side. Every map must have a table/ranking
alternative in future UI.

